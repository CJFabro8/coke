local library = loadstring(game:HttpGet('https://raw.githubusercontent.com/cypherdh/VanisUILIB/main/.gitignore'))()

local Window = library:CreateWindow("Name", "Version", 10044538000)

local Tab = Window:CreateTab("Scripts")

local Page = Tab:CreateFrame("Answers")

Label = Page:CreateLabel("this is prob the worst script")

Label = Page:CreateLabel("and i even fuckin use google😭")

Button = Page:CreateButton("Magic", "lol", function()
	local args = {
    [1] = "magic, apparently having supernatural powers. and using them in a responsible ways",
    [2] = "All"
}

game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(unpack(args))

end)

Button = Page:CreateButton("Saturn", "lol", function()
	local args = {
    [1] = "saturn has beautiful rings that circle its equator the rings are countless particles of ice that orbits the saturn independently.",
    [2] = "All"
}

game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(unpack(args))

end)

Button = Page:CreateButton("Luck", "lol", function()
	local args = {
    [1] = "to get luck you need to keep trying new things and listen an area where you have some experience and trust your intuition to turn bad luck into good luck all you have to do is dont dwell on the bad.",
    [2] = "All"
}

game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(unpack(args))

end)

local Page2 = Tab:CreateFrame("Stuff")

TextBox = Page2:CreateBox("Change Board Text", 10044538000, function(arg)
TextBox:UpdateBox("Put Text")
game:GetService("Workspace").SubjectBoard.SurfaceGui.TextLabel.Text = "arg"
end)
